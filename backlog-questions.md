# Backlog — open questions

Answer any of these whenever you get to them. Nothing here blocks me: each one
corresponds to a line in `backlog.txt` that either holds a `?` (the tool drops
it, so it never becomes data) or a note asking you to confirm a reading.

Answer by number. You don't have to do them in order or all at once.

---

## Notation — answer once, applies everywhere

**Q1.** `3+1+F ×270` (24.01.26 squat, third set). Is `F` a failed rep? So:
three, then one, then one you didn't get?

**Q2.** Confirmed already, restating so it's in one place: `(2+3)×70` and
`(6+2)×195` are rest-pause sets, `BURP` is burpees, `+BW` is bodyweight only.
Correct?

---

## Individual cells

**Q3.** 18.12.25 first chin reads `9+7×35` — nine sets of seven with 35 lb
added. That's a lot of volume next to the other days. Confirm or correct.

**Q4.** 03.01.26 deadlift: warm-up shows `4×315` as a single, and two work sets
at different weights (`1×360`, `5×335`). Both look odd. Check the page.

**Q5.** 13.01.26 ROM deadlift: warm-up `4×315` again, and the work sets descend
(`4×315`, `2×315`, `2×315`). Descending is plausible for a hard day — confirm.

**Q6.** 20.12.25 and 23.12.25 both hold only a dash in the session cell. I've
recorded them as bag 12×3 by pattern. Correct them or tell me to delete.

---

## Units — the one that matters most

**Q7.** I read your bodyweight column as going **kg → lb → kg**:

| Period | Unit | Evidence |
|---|---|---|
| Nov 2025 | kg | 14.11.25 reads `77` |
| Dec 25 – Feb 26 | lb | bodyweight 168–174, squats 245–315 |
| Mar 26 on | kg | bodyweight 76.7–80.7, squats 110–135 |

If that's wrong, every weight in the middle block is wrong by 2.2×. Please
confirm before I import.

---

## Batch 2 — IMG_1041 (Feb 2026)

**Q8.** 05.02.26 squat, middle work set: hidden under a binder ring. The outer
two are `5×290`. Was the middle one also `5×290`?

**Q9.** 05.02.26 also has a bench block — warm-up `10×45, 3×135, 1×185` is
clear, but its work sets run into the next column and I can't tell which
numbers belong to which day. What did you bench that day?

**Q10.** 08.02.26 starts on this page (squat, warm-up `10×45, 5×135`) and
continues onto the next one. I'll pick it up in batch 3 rather than guess.

### New in this batch — no action needed, just so you can see it
- 02.02.26 is a full Day 1: squat `5×280`×3, press `7×135`×3, deadlift `5×355`.
- 03.02.26 bodyweight **172**.
- The chin/dip/Liu days now read cleanly as two blocks — one heavier set,
  then five (or four) back-off sets — which matches Nov–Jan.
